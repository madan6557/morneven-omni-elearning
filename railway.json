{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && node scripts/prepare-railway-prisma.mjs && pnpm --filter @repo/db exec prisma generate && pnpm --filter api build"
  },
  "deploy": {
    "preDeployCommand": "node scripts/prepare-railway-prisma.mjs && pnpm --filter @repo/db exec prisma db push --skip-generate",
    "startCommand": "pnpm --filter api start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
