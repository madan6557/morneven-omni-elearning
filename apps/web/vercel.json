{
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [{ "source": "/((?!api(?:/|$)|uploads(?:/|$)).*)", "destination": "/index.html" }]
}
