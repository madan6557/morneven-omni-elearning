import { useEffect, useState } from "react";
import { ChevronDown, KeyRound, Pencil, Trash2, UserPlus, Users as UsersIcon } from "lucide-react";
import api from "../lib/api";
import { useFeedback } from "../context/FeedbackContext";
import { Spinner } from "../components/Loading";

const blank = { nim: "", name: "", password: "", role: "MAHASISWA" };
export default function Users() {
  const { showFeedback, requestConfirmation } = useFeedback(); const [list, setList] = useState<any[]>([]); const [search, setSearch] = useState(""); const [page, setPage] = useState(1); const [meta, setMeta] = useState({ total: 0, totalPages: 1 }); const [saving, setSaving] = useState(false); const [form, setForm] = useState(blank); const [editing, setEditing] = useState<any>(null); const [passwordUser, setPasswordUser] = useState<any>(null); const [password, setPassword] = useState(""); const [sort, setSort] = useState({ key: "createdAt", direction: "desc" });
  const load = () => api.get("/api/auth/users", { params: { search, page, limit: 25 } }).then((r) => { const data = r.data?.items ? r.data : { items: r.data, total: r.data.length, totalPages: 1 }; setList(data.items); setMeta({ total: data.total, totalPages: data.totalPages }); }).catch(() => showFeedback("Daftar user gagal dimuat.", "error"));
  useEffect(() => { const timer = window.setTimeout(load, 250); return () => window.clearTimeout(timer); }, [search, page]);
  const sorted = [...list].sort((a, b) => { const l = String(a[sort.key] || "").toLowerCase(); const r = String(b[sort.key] || "").toLowerCase(); return (l > r ? 1 : l < r ? -1 : 0) * (sort.direction === "asc" ? 1 : -1); }); const changeSort = (key: string) => setSort((s) => s.key === key ? { key, direction: s.direction === "asc" ? "desc" : "asc" } : { key, direction: "asc" });
  const saveNew = async () => { if (!(await requestConfirmation(`User ${form.name || form.nim} akan ditambahkan ke sistem.`))) return; setSaving(true); try { await api.post("/api/auth/users", form); setForm(blank); await load(); showFeedback("User berhasil ditambahkan."); } catch (e: any) { showFeedback(e.response?.data?.message || "User gagal ditambahkan.", "error"); } finally { setSaving(false); } };
  const saveEdit = async () => { setSaving(true); try { await api.put(`/api/auth/users/${editing.id}`, { nim: editing.nim, name: editing.name, role: editing.role }); setEditing(null); await load(); showFeedback("Data user berhasil diperbarui."); } catch (e: any) { showFeedback(e.response?.data?.message || "Data user gagal diperbarui.", "error"); } finally { setSaving(false); } };
  const changePassword = async () => { if (password.length < 8) return; setSaving(true); try { await api.patch(`/api/auth/users/${passwordUser.id}/password`, { password }); setPasswordUser(null); setPassword(""); showFeedback("Password user berhasil diubah."); } catch (e: any) { showFeedback(e.response?.data?.message || "Password gagal diubah.", "error"); } finally { setSaving(false); } };
  const remove = async (user: any) => { if (!(await requestConfirmation(`Akun ${user.name} akan dihapus permanen.`))) return; try { await api.delete(`/api/auth/users/${user.id}`); await load(); showFeedback("User berhasil dihapus."); } catch (e: any) { showFeedback(e.response?.data?.message || "User gagal dihapus.", "error"); } };
  return <div className="mx-auto max-w-6xl space-y-8"><div><p className="eyebrow">People & access</p><h1 className="mt-2 text-2xl font-bold sm:text-3xl">Kelola user</h1><p className="mt-2 text-sm leading-6 text-zinc-500">Edit identitas, role, password, dan status akun pengguna.</p></div><section className="section-card space-y-5"><div className="flex items-start gap-3"><UserPlus className="mt-1 text-violet-600" size={20} /><div><h2 className="font-semibold">Tambah user baru</h2><p className="mt-1 text-sm text-zinc-500">Lengkapi identitas dasar dan tentukan role akses.</p></div></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><input value={form.nim} onChange={(e) => setForm({ ...form, nim: e.target.value })} placeholder="NIM / identifier" className="field" /><input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Nama lengkap" className="field" /><input value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="Password" type="password" className="field" /><RoleSelect value={form.role} setValue={(role) => setForm({ ...form, role })} /></div><button disabled={saving} onClick={saveNew} className="primary-button"><UserPlus size={17} />{saving ? "Menyimpan..." : "Tambah user"}</button></section><section className="table-shell overflow-x-auto"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-zinc-100 px-5 py-4"><div className="flex items-center gap-3"><UsersIcon size={18} className="text-violet-600" /><div><h2 className="text-sm font-semibold">Daftar pengguna</h2><p className="text-xs text-zinc-500">{meta.total} akun · Online = aktivitas 5 menit terakhir</p></div></div><input className="field max-w-xs" placeholder="Cari nama atau NIM..." value={search} onChange={(e) => { setSearch(e.target.value); setPage(1); }} /></div><table className="w-full min-w-[950px] text-sm"><thead><tr><th><Sort label="NIM" field="nim" sort={sort} change={changeSort} /></th><th><Sort label="Nama" field="name" sort={sort} change={changeSort} /></th><th><Sort label="Role" field="role" sort={sort} change={changeSort} /></th><th>Status</th><th>Terakhir akses</th><th>Aksi</th></tr></thead><tbody>{sorted.map((u) => <tr key={u.id}><td>{u.nim}</td><td className="font-semibold">{u.name}</td><td><span className="rounded-full bg-violet-100 px-2 py-1 text-xs font-bold text-violet-700">{u.role}</span></td><td><span className={u.online ? "text-emerald-600" : "text-zinc-500"}>{u.online ? "● Online" : "● Offline"}</span></td><td className="whitespace-nowrap text-xs text-zinc-500">{u.lastSeenAt ? new Date(u.lastSeenAt).toLocaleString() : "Belum tercatat"}</td><td><div className="flex gap-2"><button className="secondary-button px-2 py-1.5 text-xs" onClick={() => setEditing({ ...u })}><Pencil size={14} /> Edit</button><button className="secondary-button px-2 py-1.5 text-xs" onClick={() => { setPasswordUser(u); setPassword(""); }}><KeyRound size={14} /> Password</button><button className="secondary-button px-2 py-1.5 text-xs text-red-600" onClick={() => remove(u)}><Trash2 size={14} /> Hapus</button></div></td></tr>)}</tbody></table>{meta.totalPages > 1 && <div className="flex items-center justify-between border-t border-zinc-100 px-5 py-3 text-xs text-zinc-500"><button disabled={page === 1} onClick={() => setPage(page - 1)} className="secondary-button px-2 disabled:opacity-40">Sebelumnya</button><span>Halaman {page} dari {meta.totalPages}</span><button disabled={page >= meta.totalPages} onClick={() => setPage(page + 1)} className="secondary-button px-2 disabled:opacity-40">Berikutnya</button></div>}</section>{editing && <UserModal user={editing} setUser={setEditing} onCancel={() => setEditing(null)} onSave={saveEdit} saving={saving} />}{passwordUser && <PasswordModal user={passwordUser} password={password} setPassword={setPassword} onCancel={() => setPasswordUser(null)} onSave={async () => { if (await requestConfirmation(`Password ${passwordUser.name} akan diganti.`)) await changePassword(); }} saving={saving} />}</div>;
}
function Sort({ label, field, sort, change }: any) { return <button className="font-semibold" onClick={() => change(field)}>{label} {sort.key === field ? (sort.direction === "asc" ? "↑" : "↓") : "↕"}</button>; }
function RoleSelect({ value, setValue }: { value: string; setValue: (role: string) => void }) { return <label className="block space-y-2 text-sm font-medium">Role pengguna<span className="select-shell"><select aria-label="Role pengguna" value={value} onChange={(e) => setValue(e.target.value)} className="field select-field"><option>MAHASISWA</option><option>DOSEN</option><option>ADMIN</option></select><ChevronDown className="select-chevron" size={17} /></span></label>; }
function UserModal({ user, setUser, onCancel, onSave, saving }: any) { return <div className="fixed inset-0 z-50 grid place-items-center bg-zinc-950/60 p-4" role="dialog" aria-modal="true"><div className="section-card w-full max-w-xl space-y-5"><div><p className="eyebrow">Administrasi user</p><h2 className="mt-2 text-xl font-bold">Edit data user</h2></div><div className="grid gap-4 sm:grid-cols-2"><input className="field" value={user.nim} onChange={(e) => setUser({ ...user, nim: e.target.value })} placeholder="NIM / identifier" /><input className="field" value={user.name} onChange={(e) => setUser({ ...user, name: e.target.value })} placeholder="Nama lengkap" /><RoleSelect value={user.role} setValue={(role: string) => setUser({ ...user, role })} /></div><div className="flex justify-end gap-3"><button disabled={saving} onClick={onCancel} className="secondary-button">Batal</button><button disabled={saving} onClick={onSave} className="primary-button">{saving ? <Spinner /> : <Pencil size={16} />} Simpan perubahan</button></div></div></div>; }
function PasswordModal({ user, password, setPassword, onCancel, onSave, saving }: any) { return <div className="fixed inset-0 z-50 grid place-items-center bg-zinc-950/60 p-4" role="dialog" aria-modal="true"><div className="section-card w-full max-w-md space-y-5"><div><p className="eyebrow">Keamanan akun</p><h2 className="mt-2 text-xl font-bold">Ganti password user</h2><p className="mt-2 text-sm text-zinc-500">{user.name} ({user.nim}) · minimal 8 karakter, maksimal 128.</p></div><input autoFocus type="password" minLength={8} maxLength={128} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Password baru" className="field" /><div className="flex justify-end gap-3"><button disabled={saving} onClick={onCancel} className="secondary-button">Batal</button><button disabled={saving || password.length < 8} onClick={onSave} className="primary-button">{saving ? <Spinner /> : <KeyRound size={16} />} Simpan password</button></div></div></div>; }
