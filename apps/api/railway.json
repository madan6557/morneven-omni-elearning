{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && node ../../scripts/prepare-railway-prisma.mjs && npx prisma generate --schema=../../packages/db/prisma/schema.prisma && pnpm build"
  },
  "deploy": {
    "preDeployCommand": "node ../../scripts/prepare-railway-prisma.mjs && node ../../scripts/railway-db-push.mjs",
    "startCommand": "pnpm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
