{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pnpm install && npx prisma generate --schema=../../packages/db/prisma/schema.prisma && npx prisma migrate deploy --schema=../../packages/db/prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "pnpm start",
    "healthcheckPath": "/api/health"
  }
}
